# yaml-language-server: $schema=../../schemas/yaml/services_schema.yaml
# Service actions for LIXIL Bluetooth Shutter integration
# Standard cover actions (open/close/stop) are handled by the Cover platform.
